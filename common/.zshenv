# zsh reads .zshenv first, for EVERY shell. Keep it minimal and OS-agnostic.
export ZDOTDIR="$HOME/.config/zsh"

# Editor resolved via PATH (each OS overlay puts the right nvim on PATH).
export EDITOR=nvim
