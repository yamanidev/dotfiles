# Linux-specific Kitty config.
#
# "monospace" is a fontconfig alias (Linux only); resolves to the system mono.
font_family monospace

# GNOME Terminal-style tab switching.
map ctrl+page_up   previous_tab
map ctrl+page_down next_tab
